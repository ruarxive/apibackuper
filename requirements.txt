# apibackuper - Core Dependencies
# Python 3.6+ required
#
# Install with: pip install -r requirements.txt
# For development: pip install -r requirements-dev.txt

# Core required dependencies
# These are the minimum dependencies needed to run apibackuper
typer>=0.9.0
lxml>=4.9.3
urllib3>=2.0.6
requests>=2.31.0
xmltodict>=0.13.0
PyYAML>=6.0
jsonschema>=4.0.0
tqdm>=4.66.0

# Optional but recommended dependencies
# These enhance functionality but are not strictly required
# aria2p: Faster file downloads using aria2
aria2p>=0.11.3
# pandas & pyarrow: Required for Parquet export format
pandas>=2.0.0
pyarrow>=14.0.1
numpy>=1.22.2 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
